# Task Board Project

## Overview

タスク管理ボードアプリケーション。

## Git 運用ルール

**コードを変更するたびに、必ず GitHub へプッシュすること。**

具体的な手順:

1. 変更をステージング: `git add <変更ファイル>`
2. コミット: `git commit -m "<メッセージ>"`
3. プッシュ: `git push origin <ブランチ名>`

### コミットメッセージ規約

- `feat:` — 新機能追加
- `fix:` — バグ修正
- `refactor:` — リファクタリング
- `docs:` — ドキュメント変更
- `test:` — テスト追加・修正
- `chore:` — ビルド設定・依存関係など

例: `feat: タスクの優先度フィルター機能を追加`

### ブランチ戦略

- `main` — 本番相当のブランチ。直接コミット禁止
- `feature/<機能名>` — 新機能開発
- `fix/<バグ名>` — バグ修正
- 作業完了後は Pull Request を作成してマージする

## 開発ルール

- 変更後は必ずテストを実行してから push する
- `.env` ファイルは絶対にコミットしない
- 未使用のコードは残さず削除する
- コメントは「なぜそうしたか」が自明でない場合のみ追加する
